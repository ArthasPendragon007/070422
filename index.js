function index(converters) {
const indexion = process.argv[2]; // Pega a 3ª entrada de uma lista de argumentos
const {converts} = require('./converts.js')

if indexion == 1;
    console.log("ola mundo")


}